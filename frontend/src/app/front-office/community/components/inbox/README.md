# Inbox Component

`InboxComponent` lists the current user's direct-message conversations.

## Responsibilities

- Load the inbox for the authenticated user
- Show unread counts and latest message summaries
- Route into `ChatWindowComponent` for a selected conversation

## Dependencies

- `MessagingService`
- `AuthService`
- Angular router
