spring.application.name=backend
spring.datasource.url=jdbc:mysql://localhost:3306/Elif?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

# Server configuration
server.port=8087
server.servlet.context-path=/elif

# Upload configuration
app.upload.base-dir=uploads
file.upload-dir=./uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ? PDF storage ? dossier où Spring sauvegarde les contrats PDF
app.contracts.dir=./uploads/contracts

# ? Spring Mail ? Gmail SMTP
# IMPORTANT : remplacez par votre email Gmail et App Password
# Pour créer un App Password Gmail :
# https://myaccount.google.com/apppasswords
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=zouarieya18@gmail.com
spring.mail.password=tlwfjppoyosxtble
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=3000
spring.mail.properties.mail.smtp.writetimeout=5000
# Pour les logs email (debug)
logging.level.org.springframework.mail=DEBUG
